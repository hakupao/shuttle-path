---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  date: Date;
  description?: string;
}

const { title, date, description } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <!-- Lesson content is fully free-form — only nav/footer from BaseLayout -->
  <slot />
</BaseLayout>
