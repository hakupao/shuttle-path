# 09 — 待交付素材清单

> 当前代码中使用了占位 SVG，以下素材需要你委托画师或 AI 完成后替换。
> 每项都标注了：用途、存放路径、替换位置、英文 prompt。

---

## 素材存放规则

所有素材统一放在 `public/images/` 目录下：

```
public/
└── images/
    ├── icons/          ← 手绘图标（分类图标、箭头等）
    ├── illustrations/  ← 大插画（Hero、404 等）
    └── decorations/    ← 装饰元素（羽毛碎片、波浪线等）
```

**格式要求：** SVG，透明背景，线条色 `#3f3a36`，`stroke-linecap: round`

---

## 1. Logo 图标（羽毛球侧视）

- **存放路径：** `public/images/icons/shuttlecock.svg`
- **替换位置：** `src/components/common/Nav.astro` 第 18-21 行的占位 SVG
- **尺寸：** 28×28px（viewBox 建议 `0 0 32 32`）
- **说明：** 导航栏品牌名左侧的小图标

**Prompt:**
> Hand-drawn style badminton shuttlecock, side view, flying to the upper-right with a slight spin. Loose ink-sketch line art, lines not perfectly closed — leave small gaps at intersections for a casual hand-drawn feel. The cork base is a rounded shape, feather skirt fans out behind it. Monochrome dark gray line art (#3f3a36), no fill, stroke width 2px, round stroke caps. SVG format, viewBox "0 0 32 32", transparent background.

---

## 2. 手法分类图标（球拍）

- **存放路径：** `public/images/icons/racket.svg`
- **替换位置：** `src/pages/index.astro` categories 数组中 `strokes` 的 `icon` 字段
- **尺寸：** 32×32px

**Prompt:**
> Hand-drawn badminton racket icon, simplified sketch style. The racket frame is an elongated oval, the string pattern represented by just 3-4 casual diagonal lines (not a full grid). The handle is short with a slight curve. Loose pen-sketch feel, lines have natural thickness variation. Dark gray line art (#3f3a36), no fill, transparent background, SVG format, viewBox "0 0 32 32".

---

## 3. 步法分类图标（脚步）

- **存放路径：** `public/images/icons/footstep.svg`
- **替换位置：** `src/pages/index.astro` categories 数组中 `footwork` 的 `icon` 字段
- **尺寸：** 32×32px

**Prompt:**
> Hand-drawn footstep / shoe print icon for a badminton footwork category. Two shoe prints, one in front and one behind, suggesting forward movement. The prints have a sporty shoe sole pattern (simple tread marks). Loose sketch style with natural line variation. Include a small curved arrow between the prints to suggest dynamic stepping motion. Dark gray line art (#3f3a36), no fill, transparent background, SVG format, viewBox "0 0 32 32".

---

## 4. 运动科学分类图标（心率）

- **存放路径：** `public/images/icons/sports-science.svg`
- **替换位置：** `src/pages/index.astro` categories 数组中 `sports-science` 的 `icon` 字段
- **尺寸：** 32×32px

**Prompt:**
> Hand-drawn sports science icon combining a simplified human figure in a lunging athletic pose with a heartbeat/pulse line running across the composition. The figure is a minimal stick-figure style but with slightly thicker, expressive strokes (not rigid). The pulse line weaves through or behind the figure. Loose sketch style, energetic feel. Dark gray line art (#3f3a36), no fill, transparent background, SVG format, viewBox "0 0 32 32".

---

## 5. Hero 主视觉插画

- **存放路径：** `public/images/illustrations/hero-shuttlecock.svg`
- **替换位置：** `src/pages/index.astro` 第 87-98 行的占位 SVG
- **尺寸：** 约 400×400px（viewBox 建议 `0 0 400 400`）
- **说明：** 首页右上角的大装饰插画，桌面端显示

**Prompt:**
> Large hand-drawn illustration of a badminton shuttlecock in dynamic flight, for a website hero section. The shuttlecock is moving diagonally upward-right with energy and speed. Behind it trails a sweeping curved arc (the flight path) drawn with a confident, varied-thickness brush stroke — thick at the start, tapering to thin. 2-3 small feather fragments drift off the trail. Around the shuttlecock, add a few quick speed lines and a subtle starburst/impact mark suggesting it was just hit. The overall composition should feel dynamic and athletic, but with the warmth of a hand-drawn sketch. Main line art in dark gray (#3f3a36), the flight arc trail in semi-transparent green (#10B981 at 40% opacity), feather fragments in lighter gray. No background, SVG format, viewBox "0 0 400 400".

---

## 6. 404 页面插画

- **存放路径：** `public/images/illustrations/404-out-of-bounds.svg`
- **替换位置：** `src/pages/404.astro` 第 8-22 行的占位 SVG
- **尺寸：** 约 240×180px（viewBox 建议 `0 0 240 180`）

**Prompt:**
> Hand-drawn illustration of a badminton shuttlecock that has flown way out of bounds. The composition shows: a partial badminton court boundary line (dashed, hand-drawn) in the lower-left corner, and the shuttlecock far in the upper-right, with an exaggerated curved dotted flight path (arc trajectory) connecting them. The shuttlecock is tumbling/spinning in the air. Add a few motion lines and scattered feather fragments near the shuttlecock. The court boundary line has a small hand-written "OUT!" label next to it. Humorous, lighthearted tone. Dark gray line art (#3f3a36), stroke width 2px, round caps. Optional: the flight arc in green (#10B981), the "OUT" text in orange (#F59E0B). Transparent background, SVG format, viewBox "0 0 240 180".

---

## 7. 波浪分隔线（3-4 种变体）

- **存放路径：** `public/images/decorations/divider-1.svg`, `divider-2.svg`, `divider-3.svg`
- **替换位置：** `src/styles/global.css` 中 `.divider-wavy` 的 `background-image`
- **尺寸：** 宽 400px，高 12px（水平平铺使用）
- **说明：** 当前用 CSS 生成的简单波浪线占位，效果比较机械

**Prompt:**
> Set of 3 hand-drawn wavy horizontal divider lines, each with a different wave pattern. These are section separators for a website. The waves should feel like a quick pen stroke — not perfectly sinusoidal but organic and slightly irregular. Varying stroke thickness along the line (thicker in the middle of a stroke, thinner at the ends). One should be more gentle/subtle, one more playful with tighter waves. Dark gray line art (#3f3a36), stroke width 1.5-2px, round caps, SVG format, viewBox "0 0 400 12", horizontally tileable.

---

## 8. 羽毛碎片装饰（3 片）

- **存放路径：** `public/images/decorations/feather-1.svg`, `feather-2.svg`, `feather-3.svg`
- **替换位置：** `src/components/common/Footer.astro` 第 5-10 行的占位 SVG
- **尺寸：** 每片约 24-40px
- **说明：** Footer 角落的半透明装饰

**Prompt:**
> 3 individual loose feather fragments, hand-drawn sketch style, as if they drifted off a badminton shuttlecock. Each feather is slightly different — one curving left, one floating straight, one with a slight twist. Loose, airy line work with gentle curves. Think of feathers caught in mid-air. Dark gray line art (#3f3a36), delicate thin strokes (1-1.5px), no fill, transparent background, SVG format. Each feather as a separate file.

---

## 9. 教学日记空状态插画

- **存放路径：** `public/images/illustrations/empty-lessons.svg`
- **替换位置：** `src/pages/lessons/index.astro` 空状态区域的占位图标
- **尺寸：** 约 128×128px

**Prompt:**
> Hand-drawn open notebook with a pen resting on it, sketch illustration style. The notebook is open flat showing lined pages, one page has a few casual squiggle lines suggesting handwritten notes. A pen or pencil lies diagonally across the pages. The notebook has a slightly rumpled, lived-in feel — pages not perfectly flat, spine slightly curved. Warm, inviting atmosphere suggesting "stories about to be written." Dark gray line art (#3f3a36), stroke width 2px, optional very light watercolor-style green (#10B981 at 15% opacity) wash on the notebook cover. Transparent background, SVG format, viewBox "0 0 128 128".

---

## 如何替换

拿到 SVG 文件后：

1. **图标类**（Logo、分类图标）：打开 SVG 文件，复制 `<svg>` 标签内的内容，粘贴到代码中对应位置替换占位 SVG
2. **插画类**（Hero、404）：放到 `public/images/illustrations/`，然后在代码中用 `<img src="/images/illustrations/xxx.svg" />` 引用，或内联 SVG
3. **装饰类**（波浪线、羽毛碎片）：放到 `public/images/decorations/`，在 CSS 或 HTML 中引用

**优先级建议：**
1. **最先做 → Logo + 分类图标**（全站最常见的元素）
2. **其次 → Hero 插画 + 波浪分隔线**（首页视觉冲击力）
3. **最后 → 404 插画 + 羽毛碎片 + 空状态**（次要页面）

---

## 通用风格指引

**交付给画师/AI 时附上这段：**

> **Overall style guide for Shuttle Path icon set:**
>
> - Style: Hand-drawn ink sketch, like quick confident pen strokes on paper
> - Line quality: Natural thickness variation (thicker on downstrokes, thinner on curves), not perfectly uniform
> - Line gaps: Occasionally leave small gaps where lines meet — this creates the casual hand-drawn feel
> - Color: Primary line color is dark warm gray #3f3a36. Accent colors (when specified) are green #10B981 and orange #F59E0B, used sparingly at low opacity (15-40%) as watercolor-style washes
> - Stroke: Round line caps (stroke-linecap: round), stroke width 1.5-3px depending on icon size
> - Format: SVG with transparent background, clean paths, no embedded raster images
> - Feel: Athletic but warm. Like a coach's whiteboard sketches — quick, clear, with personality. Not cute/kawaii, not rigid/corporate.
